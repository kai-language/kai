os:
  - osx
language: generic
sudo: required
osx_image: xcode8
before_install:
  - brew update
install:
  - brew install homebrew/versions/llvm36
script:
  - set -o pipefail
  - swift build -Xswiftc -I/usr/local/opt/llvm/include -Xlinker -L/usr/local/opt/llvm/lib
