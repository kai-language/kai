#library "c" libc

print :: (string, ..any) -> void #foreign libc "printf"
length :: (string) -> u64 #foreign libc "strlen"

main :: () -> void {
    array : [5]int = { 1, 2, 3, 4, 5 }

    for i := 0; i < 5; i += 1 {
        print("array[%d] = %d\n", i, array[i])
    }

}

