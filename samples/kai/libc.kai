#library "c" libc

fopen :: (filename: *u8, mode: *u8) -> rawptr #foreign libc "fopen"
fclose :: (ptr: rawptr) -> i32 #foreign libc "fclose"
fread :: (ptr: rawptr, size: u64, nmemb: u64, file: rawptr) -> i64 #foreign libc "fread"
