#library "c" libc

len :: (str: string) -> u64 {
    return <(*u64)(&str[-16])
}

cap :: (str: string) -> u64 {
    return <(*u64)(&str[-8])
}

main :: () -> void {
    buffer: [255]i8

    ptr := (*i8)(buffer)
}
