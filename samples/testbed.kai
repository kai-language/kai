#library "c" libc

print :: (string, ..any) -> void #foreign libc "printf"

main :: () -> void {
    age : int = 23

    switch {
    case age < 10: print("hi\n")
    case age == 23: print("Me, too!\n")
    default:
        print("Defaulted\n")
    }
}
