#library "c" libc

#import "string.kai" str 

test :: () -> ^u64 {
    return nil
}

main :: () -> void {
    name := "Brett"
    length := str.len(name)

    printf("%s is %llu bytes long\n", name, length)
}
