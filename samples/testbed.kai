#library "c" libc

print :: (string, ..any) -> void #foreign libc "printf"

main :: () -> void {
    a := "Hello, world!"
    b := malloc(14)
    defer free(b)

    print("Before memcpy: %s\n", b)
    memcpy((*void)(b), (*void)(a), 14)
    print("After: %s\n", b)
}
