
/// Matches procedures with the signature `(T) -> T` | `(*T)`
prefix operator -

infix operator + //{ associativity left precedence 50 }
infix operator - //{ associativity left precedence 50 }
//infix operator * //{ associativity left precedence 60 }
//infix operator / //{ associativity left precedence 60 }

x := 0
if true { x = 5 }
else { x = 8 }

y := -x + 5

