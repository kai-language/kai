
#library "c" libc

str :: "Hello, Kai!"

isNonZero :: (n: int, e: int) -> bool {
    if n return true
    else return false
}

canFail :: (i: int, j: int) -> int, bool {
    if false return i, false
    else return j, false
}

print :: (s: string) -> void #foreign libc "printf"

main :: () -> void {
    x := 5
    y := x

    return
}

/*
entities: 4
types: 4
definitions: 4
uses: 1
scopes: 1
*/
