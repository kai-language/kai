
str :: "Hello, Kai!"

foo :: (n: int) -> int { return n }

main :: () -> void {
    x := 5
    bar := (n: int) -> int { return !n }
    foo(x)
    bar(1)  // Incompatible type for argument, expected 'int' but got 'unconstrString'
    return
}
