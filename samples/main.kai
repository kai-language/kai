#library "c" libc

print :: (string, ..any) -> void #foreign libc "printf"

// Program entry point
main :: () -> void {

    foo :: (n: int) -> void {
        print("foo printing %d\n", n)
    }

    foo(228934)
}

