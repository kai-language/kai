#library "c" libc

#import "globals.kai" .

print :: (string, ..any) -> void #foreign libc "printf"

// Program entry point
main :: () -> void {

    if tau > pi {
        print("tau (%f) is greater than pi (%f)\n", tau, pi)
    }
}

