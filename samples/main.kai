#library "c" libc

#import "globals.kai"

print :: (string, ..any) -> void #foreign libc "printf"

// Program entry point
main :: () -> void {

    if globals.tau > globals.pi {
        print("tau (%f) is greater than pi (%f)\n", globals.tau, globals.pi)
    }
}

